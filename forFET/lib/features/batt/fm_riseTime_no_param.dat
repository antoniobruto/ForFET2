feature fm_riseTime_no_param;
begin
	var t1,t2;
	(state==precharge) && first_match(@+(v>=0.1*Vterm)), time=0, t1=$time 
		##[0:$]
			first_match ( (state==constantCurrent) ) && @+(v>=0.9*Vterm) ,t2=$time
	|-> fm_riseTime_no_param = t2 -t1;
end
